## План работы

---

### Шаг 1 — Собрать данные для нормализации
**Что сделать:**
- Открыть 10–15 последних заказов в insales
- Выписать как выглядит поле «город/НП» — точное написание которое приходит в API
- Открыть список обслуживаемых НП от ТК (118 городов + посёлки основной сети)
- Выписать 10–15 примеров написания из этого списка

**Результат:** два списка примеров для сравнения

---

### Шаг 2 — Подготовить список основной сети
**Что сделать:**
- Взять 118 городов + посёлки основной сети из кабинета ТК
- Сохранить в JSON файл в проекте на Netlify
- Определить финальный формат хранения — строки или коды если найдётся корреляция

**Результат:** `main-network.json` рядом с функцией

---

### Шаг 3 — Написать функцию нормализации
**Что сделать:**
- На основе примеров из Шага 1 написать функцию которая приводит оба источника к единому виду
- Покрыть типичные расхождения: «г.», «г », «город», регистр, пробелы
- Проверить на реальных примерах из заказов

```javascript
const normalize = (str) => str
  .toLowerCase()
  .replace(/^(г\.|г |город |д\.|дер\.|аг\.)/i, '')
  .trim();
```

**Результат:** рабочая функция нормализации под ваши реальные данные

---

### Шаг 4 — Обновить API на Netlify
**Что сделать:**
- Загрузить `main-network.json` один раз при старте функции в `Set`
- Добавить определение `isRural` через нормализацию + проверку в Set
- Добавить недостающие компоненты в расчёт доставки:

```javascript
const codFee         = orderSum * 0.015;   // наложенный платёж
const declaredValue  = orderSum * 0.01;    // объявленная ценность
const ruralSurcharge = isRural ? 4 : 0;    // надбавка за деревню
const packagingFee   = 2.5;               // упаковка
```

**Результат:** API считает полную реальную стоимость доставки

---

### Шаг 5 — Тестирование до запуска
**Что сделать:**
- Оформить 5–6 тестовых заказов вручную:
  - город основной сети + разные суммы
  - деревня + разные суммы
  - посёлок пограничный случай
- Сравнить что показывает API с реальным тарифом ТК
- Скорректировать коэффициенты если есть расхождение

**Результат:** уверенность что расчёт правильный перед запуском в продакшн

---

### Шаг 6 — Запуск и мониторинг
**Что сделать:**
- Задеплоить обновлённую функцию на Netlify
- В течение первых 2 недель сезона сверять акты ТК с тем что показывал API
- Логировать расхождения — завести простой лог:

```
дата | НП | тип НП (API) | стоимость API | стоимость ТК | разница
```

**Результат:** данные для точной калибровки после сезона

---

### Шаг 7 — Калибровка после сезона
**Что сделать:**
- Выгрузить лог расхождений
- Посчитать среднее расхождение по городу и по деревне отдельно
- Скорректировать коэффициенты в формуле

**Результат:** точный расчёт на следующий сезон

---

## Порядок приоритетов

```
Сегодня:   Шаг 1 — данные (30 мин)
Сегодня:   Шаг 4 частично — добавить codFee + declaredValue + packagingFee
           (это не зависит от нормализации, можно сделать прямо сейчас)
Завтра:    Шаги 2–3 — список НП + нормализация
Завтра:    Шаг 4 полностью + Шаг 5 — тестирование
После:     Шаг 6 — запуск
Конец сезона: Шаг 7 — калибровка
```

---

Шаг 4 (часть без нормализации) можно сделать прямо сейчас — он не зависит от остального и сразу уберёт основную часть расхождений с актами ТК. Скидывайте код вашей текущей функции — помогу добавить.